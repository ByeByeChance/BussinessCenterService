# 配置数据库连接信息
data_source:
  host: 'localhost'
  port: 3306
  user: 'root'
  password: 'chance..'
  database: 'business'
# 配置字段收取及基础类(可选，如果没有配置的话，那么就是默认每个实体类都带了相同的字段)
data_config:
  collect: 'id, createdTime, updatedTime, deletedTime'
  base_name: 'SharedEntity'
